# Leave empty for the deployed same-origin setup so the browser calls /api on
# https://ceo-workforce-index.com. Override this locally when running the API
# on a separate development port.
VITE_CWI_API_BASE_URL=
VITE_CWI_SUBMIT_TIMEOUT_MS=15000